import { useCreateReference } from "./useCreateReference";
import { useUpdateReference } from "./useUpdateReference";
import { useReference, useLazyReference } from "./useReference";
import { useLazyReferences, useReferences } from "./useReferences";

export {
    useCreateReference,
    useUpdateReference,
    useReference,
    useLazyReference,
    useLazyReferences,
    useReferences
}